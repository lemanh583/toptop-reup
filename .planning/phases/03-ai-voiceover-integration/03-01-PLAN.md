---
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/services/voiceover.py
  - backend/services/transform.py
  - backend/tasks.py
  - backend/api/routes/voiceover.py
  - backend/main.py
autonomous: true
---

# Plan: 03-01 AI Voiceover Integration

## Objective
Xây dựng engine Voiceover dùng `edge-tts` để sinh Text-to-Speech, và dùng FFMPEG mix luồng audio AI vào video (giữ lại 15% audio cũ làm nhạc nền, loop TTS đến hết video).

## Tasks

### 1. Requirements updates
<read_first>
- backend/requirements.txt
</read_first>
<action>
Thêm `edge-tts>=6.1.12` vào `backend/requirements.txt`.
</action>
<acceptance_criteria>
- `requirements.txt` có `edge-tts`.
</acceptance_criteria>

### 2. Edge-TTS Service
<read_first>
- N/A
</read_first>
<action>
Tạo `backend/services/voiceover.py` với class `VoiceoverService`.
Cung cấp phương thức `generate_tts_sync(text, output_mp3_path)` để chạy TTS đồng bộ/async bọc thành sync cho dễ gọi từ Celery. Sử dụng CLI `edge-tts --voice vi-VN-HoaiMyNeural --text "..." --write-media ...` qua subprocess.
</action>
<acceptance_criteria>
- File `voiceover.py` cung cấp module giao tiếp Edge-TTS.
</acceptance_criteria>

### 3. FFMpeg Audio Muxing
<read_first>
- backend/services/transform.py
</read_first>
<action>
Vào `backend/services/transform.py`, thêm method `apply_audio_mux(video_path, audio_path, output_path)` cho `VideoTransformer`. Cấu hình FFmpeg trộn 2 audio: original (volume=0.15), và TTS (loop=vô hạn, volume=1.5). Xuất file output.
</action>
<acceptance_criteria>
- Hàm `apply_audio_mux` chứa lệnh mix ffmpeg volume và aloop.
</acceptance_criteria>

### 4. Voiceover Celery Task
<read_first>
- backend/tasks.py
</read_first>
<action>
Vào `backend/tasks.py`, thêm task `@celery_app.task(bind=True, name="voiceover_video_task")` nhận `video_id` và `script`. Chạy sinh âm thanh mp3 -> Chạy ffmpeg phối trộn vào video mp3 -> Lưu kết quả `_voiceover.mp4` -> Update status SUCCESS.
</action>
<acceptance_criteria>
- Có khai báo `voiceover_video_task`.
</acceptance_criteria>

### 5. API Endpoint
<read_first>
- backend/api/routes/voiceover.py
- backend/main.py
</read_first>
<action>
Tạo `backend/api/routes/voiceover.py` nhận `POST /api/voiceover` payload `{"video_id": "...", "script": "..."}` và gởi Celery queue.
Khai báo nó vào `backend/main.py`.
</action>
<acceptance_criteria>
- API routes `/api/voiceover` có sẵn trên main.py.
</acceptance_criteria>

## Verification
- Code python compile pass. 
