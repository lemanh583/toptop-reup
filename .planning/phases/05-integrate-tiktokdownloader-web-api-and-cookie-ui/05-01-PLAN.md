---
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.jsx
  - backend/api/routes/download.py
  - backend/tasks.py
autonomous: true
---

# Plan: 05-01 Web API Integration

## Objective
Sửa Frontend đẩy cookie, sửa Router nhận cookie và cập nhật Tasks xử lý payload gọi Web API cổng 5555.

## Tasks

### 1. Update Frontend UI
<read_first>
- frontend/src/App.jsx
</read_first>
<action>
Thêm ô nhập `Cookie` ngay trên form nhập `URL` trong UI.
Sửa nút Tải Video để truyền object `{ url, cookie }` khi gọi axios.
</action>

### 2. Update Backend Route
<read_first>
- backend/api/routes/download.py
</read_first>
<action>
Update Pydantic BaseModel `DownloadRequest`:
```python
class DownloadRequest(BaseModel):
    url: str
    cookie: str | None = None
```
Truyền parameter `cookie` vào `download_tiktok_video.delay(request.url, request.cookie)`.
</action>

### 3. Rewrite Celery Task Call
<read_first>
- backend/tasks.py
</read_first>
<action>
Cập nhật function `download_tiktok_video` để gọi API giải mã `/douyin/detail` (Hoặc `/tiktok/detail` tuỳ loại link). Trích xuất video URL thật, bóc tách httpx Client để lưu stream MP4. Giữ nguyên đoạn return code `{"message": "Success"}` để tracking UI không bị gãy.
</action>

## Verification
- Code không syntax error.
- UI React có ô nhập Cookie mượt mà.
